1: Largest & Smallest Element in Array
		public class Day01_LargestSmallest {
    public static void main(String[] args) {
        int[] arr = {4, 2, 9, 1, 7};

        int min = arr[0], max = arr[0];

        for (int num : arr) {
            if (num < min) min = num;
            if (num > max) max = num;
        }

        System.out.println("Min: " + min);
        System.out.println("Max: " + max);
   		 }
		}
 Time: O(n) | Space: O(1)
